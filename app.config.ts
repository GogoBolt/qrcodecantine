export default defineAppConfig({
  ui: {
    primary: 'green',
    gray: 'slate',
    notifications: {
      // Show notifications on the top right of the screen
      position: 'top-right'
    },
    icons: {
      dynamic: true
    }
  }
})
